/* noop.js — ADAPT stealth plane. Empty success response for first-party bait
 * scripts (e.g. /ads.js). Loads cleanly (onload fires, no global side effects)
 * so naive load-failure detectors settle as "not detected". */
(function () { 'use strict'; })();
